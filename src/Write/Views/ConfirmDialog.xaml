<Window x:Class="BeexWrite.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BeexWrite" Width="420" Height="180"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{DynamicResource Bx.WindowBackground}"
            BorderBrush="{DynamicResource Bx.Border}" BorderThickness="1"
            CornerRadius="8" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="MessageText" Grid.Row="0" TextWrapping="Wrap"
                       Foreground="{DynamicResource Bx.Text}" FontSize="14"
                       VerticalAlignment="Center" />
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="BtnThird" Width="90" Padding="10,8" Margin="0,0,10,0"
                        Click="OnThird" Visibility="Collapsed" />
                <Button x:Name="BtnCancel" Width="90" Padding="10,8" Margin="0,0,10,0"
                        Click="OnCancel" IsCancel="True" />
                <Button x:Name="BtnConfirm" Width="90" Padding="10,8"
                        Click="OnConfirm" IsDefault="True"
                        BorderBrush="{DynamicResource Bx.Accent}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
